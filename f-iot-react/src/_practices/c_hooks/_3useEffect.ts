export const tmp = '';

//! === useEffect ===

//? 1. useEffect 훅의 주요 목적은 무엇인가?
// A. 컴포넌트의 상태를 초기화하기 위해
//& B. 렌더링 후 부수효과(side effect)를 처리하기 위해
// C. CSS를 동적으로 적용하기 위해
// D. 데이터를 즉시 반환하기 위해

//? 2. useEffect는 언제 실행되는가?
//& A. 컴포넌트가 마운트될 때와 의존성 배열이 변경될 때
// B. 컴파일 시점
// C. 이벤트 발생 시점
// D. 항상 렌더링 전에

//? 3. useEffect의 두 번째 인자(의존성 배열)의 역할은?
// A. 반환값을 지정한다
//& B. 실행 조건을 제어한다
// C. JSX 구조를 정의한다
// D. 상태값을 초기화한다

//? 4. 아래 코드의 동작으로 옳은 것은?
/*
  useEffect(() => {
    console.log('mounted');
  }, []);
*/
//& A. 컴포넌트가 마운트될 때 한 번만 실행된다
// B. 의존성 배열이 없으므로 계속 실행된다
// C. 아무 것도 실행되지 않는다
// D. 에러가 발생한다

//? 5. 다음 중 무한 렌더링(loop)을 유발할 수 있는 코드는?
/*
  const [count, setCount] = useState(0);
  useEffect(() => {
    setCount(count + 1);
  });
*/
//* useEffect 안에서 setState 호출 시, 
//* 다시 렌더링 → effect 재실행 → 무한 반복
//& A. 의존성 배열이 없어서 렌더링마다 실행되어 무한 루프 발생
// B. 의존성 배열이 없으므로 한 번만 실행됨
// C. count가 없어서 에러 발생
// D. 정상 동작

//? 6. useEffect의 반환 함수(return)는 어떤 시점에 실행되는가?
// A. 컴포넌트가 마운트될 때
//# B. 언마운트되거나 다음 effect 실행 전 정리(cleanup) 시점
// C. 상태 업데이트 직후
// D. 렌더 직후 즉시

//? 7. useEffect 내부에서 비동기 함수를 직접 사용할 수 있다. (O)

//? 8. 아래 코드에서 console.log는 언제 실행되는가??
/*
  useEffect(() => {
    console.log('count changed');
  }, [count]);
*/
//& A. count 값이 변경될 때마다
// B. 한 번만
// C. 매 렌더링마다
// D. 언마운트 시

//? 9. useEffect는 동기적으로 실행된다. (X)
//& 비동기적으로 실행 → 렌더링 완료 후 실행

//? 10. useEffect는 서버 사이드 렌더링(SSR) 시 실행되지 않는다. (X)

//@ cf) 서버 사이드 렌더링(SSR)
// 서버가 HTML을 완성해서 브라우저에 보내는 렌더링 방식
// == 사용자가 페이지를 요청하는 경우 ==
// 1) 브라우저가 아니라 서버에서 React 컴포넌트를 실행
// 2) 그 결과로 만들어진 완성된 HTML 문서를 사용자에게 바로 전달

//* 클라이언트 사이드 렌더링(CSR)
// 1) 브라우저가 빈 HTML + JS 파일을 받음
// 2) React 코드가 실행되어 브라우저 안에서 화면을 그림
// 3) 초기에 로딩이 느림, 하지만 이후 빠르게 동작
// EX) React 

//^ 서버 사이드 렌더링(SSR) 
// 1) 서버가 React 코드를 실행해 HTML을 미리 그림
// 2) 완성된 HTML을 브라우저로 전송 -> 바로 화면 표시
// 3) 이후 JS가 작용되어 상호작용 가능
// EX) Next.js