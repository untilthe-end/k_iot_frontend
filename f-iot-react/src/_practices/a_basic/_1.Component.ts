export const tmp = '';

//! === 컴포넌트 (Component) ===

//? 1️ React 컴포넌트를 정의하는 올바른 방법은?
//& A. function이나 class를 사용하여 정의한다.
// B. HTML 태그로 정의한다.
// C. CSS 파일로 정의한다.
// D. JSON 객체로 정의한다.

//? 2. 컴포넌트 이름은 일반적으로 어떻게 시작해야 하는가?
// A. 소문자
// B. 숫자
//& C. 대문자
// D. 아무렇게나 가능

//? 3. React 컴포넌트 파일 확장자는 TypeScript에서 보통?
// A. .js
// B. .jsx
// C. .ts
//& D. .tsx

//? 4.아래 코드에서 잘못된 부분은?
/*
  function helloWorld() {
    return <h1>Hello</h1>;
  }
  export default helloWorld;
*/

//& A. 컴포넌트 이름이 소문자로 시작
// B. JSX 반환이 잘못됨
// C. export 문법 오류
// D. 괄호가 잘못 닫힘

//? 5. 컴포넌트는 React에서 어떤 역할을 하는가?
// A. 데이터를 영구 저장
//& B. UI를 구성하는 단위
// C. 서버 API 호출 담당
// D. CSS 전용 모듈

//? 6. React에서 컴포넌트를 조합(composition)하는 주된 목적은?
//& A. 코드 재사용성과 유지보수성 향상
// B. 속도 증가
// C. DOM 직접 조작
// D. 전역 상태 제거

//? 7. 다음 중 함수형 컴포넌트의 올바른 구조는?
//& A. const App = () => { return <div>Hi</div>; };
// B. const App = [<div>Hi</div>]
// C. const App = { return <div>Hi</div>; }
// D. function = App() { <div>Hi</div>; }

//? 8. React 컴포넌트는 반드시 무엇을 반환해야 하는가?
// A. HTML
//& B. JSX or null
// C. JSON
// D. CSS

//? 9. 컴포넌트 내부에서 다른 컴포넌트를 사용할 수 있다. (O)

//? 10. React 컴포넌트는 화면에 재사용 가능한 UI 블록을 정의한다. (O)